---
title: 待测试
description: 当前版本已实现但仍需人工验证的变更项
---

# 待测试

- 登录准入：新增网页自助注册、账号密码登录、账号配置修改、服务端会话 Cookie、页面和 API 访问拦截；账号运行数据写入 data/auth-users.json，不写入 YAML；画布、素材、生成记录和 AI API Key 仍保存在当前浏览器本地。
- Codex App 插件文档：新增插件安装教程，区分 AI 自动安装、手动安装和本仓库开发调试安装；同步优化插件 README 与技能说明。
- 配置与用户偏好：每个模型渠道新增 OpenAI / Gemini 调用格式选择，默认 OpenAI，切换默认格式时同步更新默认 Base URL；渠道 Tab 增加紧凑醒目的提醒，说明模型是否可选需要到“模型”Tab 选择。
- Gemini 调用格式：支持拉取 Gemini 模型列表，并用于文本问答、在线 Agent 工具调用、基础生图和图生图请求。
- 音频和视频生成：选择 Gemini 调用格式时先给出不支持提示，仍需使用 OpenAI 兼容渠道。
- 提示词库：接入 `songtianlun/awesome-prompts` 与 `Danielhan626/best-gpt-image-2-prompts-digest` 作为 GitHub 提示词源，可在 `/prompts` 页面按分类浏览。
- 提示词库：移除 `EvoLinkAI/awesome-gpt-image-2-API-and-Prompts` 源，避免保留已经不可正常拉取的重复来源。
- 提示词库：对应页面新增提示词总量和更新时间展示，便于确认当前加载规模和刷新时间。
- 提示词库：全量接入 `YouMind-OpenLab/ai-image-prompts-skill`，按 `references/manifest.json` 分类 JSON 解析并合并展示。
- 配置与用户偏好：Base URL 默认值、保存值和请求拼接会自动去除结尾 `/`，避免界面展示和接口请求出现多余斜杠。
- 配置与用户偏好：页面加载时会自动清理旧本地配置里的 Base URL 尾斜杠；模型拉取改由本应用 `/api/models` 服务端代理，避免浏览器直连外部 API 的 CORS 或 HTML 首页响应影响。
- AI 图片生成：OpenAI 文生图和图生图/参考图编辑改由本应用 `/api/ai` 临时转发，减少浏览器直连外部 API 的 CORS 或 HTML 首页响应导致的“请求失败”。

- 配置与生成：默认渠道模型列表只保留 `gpt-image-2`；修复 `youmind-nano-banana-pro` 提示词分类封面误显示 Language/License/Stars 等仓库徽章的问题。
- 提示词库图片：提示词卡片和详情页改用统一图片组件，图片为空或加载失败时显示占位；提示词源解析补充 HTML 图片、过滤徽章/封面/已失效源图片，并优先使用可展示的生成结果图。
- 首页展示：首页“灵机一动”区域每次刷新随机展示 12 条有封面的提示词，副标题为“每次刷新页面，都会遇见新的视觉灵感。”。
- 首页展示：“灵机一动”卡片移除图片上的提示词正文遮罩，只保留顶部轻量标签和底部标题，减少对图片主体的遮挡。
- 首页展示：“灵机一动”标题旁新增“换一批”按钮，可不刷新整页直接重新随机加载示例图。
- 首页展示：“灵机一动”每张卡片新增“复制”和“导入”动作；复制会复制提示词，导入会新建画布并放入示例图节点和已填提示词的配置节点。
- 首页展示：“灵机一动”标题旁新增“中文”按钮，只在包含中文的提示词中随机换图。
- 提示词库：筛选区新增“中文”开关，只展示标题、提示词或预览内容包含中文字符的提示词。
- 首页展示：“当前提示词总量”后追加中文提示词总量。
- 首页展示：“灵机一动”的“全部/中文”改为范围切换胶囊，“换一批”作为独立动作放在同一控制条中。
- 首页展示：“灵机一动”范围切换选中态改为浅暖色，避免黑底过重导致文字和状态识别困难。
- ???????????????????????????????????????????
- ?????????????????????????????????????????????????
- ????????? `data/prompts-cache.json` ??????????????????/????????????????????????????????
- ????????? `/api/prompts/cache` ???????????????????????????? URL ??????????????
